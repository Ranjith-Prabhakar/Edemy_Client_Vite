import { createSlice } from "@reduxjs/toolkit";

type userDataType= {[key:string]:string|number}

const initialState:{isLoading:boolean ,userData:userDataType} = {
  isLoading: true,
  userData:{}as userDataType
};

const userSlice = createSlice({
  name:"user",
  initialState,
  reducers:{
    
  }
})