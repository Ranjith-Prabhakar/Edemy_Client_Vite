keep folders for each features and keep all the files related with that
it is a mini version of source folder
so we could see duplication of all folders in src except features itself here 

authFolder 

inside the folder > components folder
                  > hook folder
                  > service folder
                  > index.js file everything in this folder will imported 
                     to this index file and exported from here 